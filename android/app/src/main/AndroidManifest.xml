<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Opositores"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.oppositions.OppositionListActivity" android:exported="false"/>
        <activity android:name=".ui.oppositions.OppositionDetailActivity" android:exported="false"/>
        <activity android:name=".ui.tests.TestCreationActivity" android:exported="false"/>
        <activity android:name=".ui.tests.TestRunActivity" android:exported="false"/>
        <activity android:name=".ui.topics.TopicDetailActivity" android:exported="false"/>
        <activity android:name=".ui.flashcards.FlashcardActivity" android:exported="false"/>

    </application>
</manifest>
